<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title><%= book ? "Editar Livro" : "Adicionar Livro" %></title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <header>
    <h1>📖 Book Notes</h1>
    <nav>
      <a href="/">Início</a> |
      <a href="/books/add">Adicionar Livro</a>
    </nav>
    <hr>
  </header>

  <main>
    <form method="POST" action="<%= book ? `/books/edit/${book.id}` : '/books/add' %>" class="book-form">
      <label>Título:</label>
      <input type="text" name="title" value="<%= book ? book.title : '' %>" required>

      <label>Autor:</label>
      <input type="text" name="author" value="<%= book ? book.author : '' %>" required>

      <label>Notas:</label>
      <textarea name="notes"><%= book ? book.notes : '' %></textarea>

      <label>Rating:</label>
      <input type="number" name="rating" value="<%= book ? book.rating : '' %>" min="0" max="5">

      <label>Data de leitura:</label>
      <input type="date" name="read_date" value="<%= book && book.read_date ? book.read_date.toISOString().split('T')[0] : '' %>">

      <button type="submit"><%= book ? "Salvar Alterações" : "Adicionar Livro" %></button>
    </form>
  </main>

  <footer>
    <hr>
    <p>Feito por Tamiris 💜</p>
  </footer>
</body>
</html>